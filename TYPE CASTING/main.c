#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum =17;
    int co = 5;
    double mean;

    mean = (double)sum/co; //Typecasting or converting the integer poutput to double
    printf("The mean is %f\n", mean);
    return 0;
}
